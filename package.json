{
  "name": "dynamic-web-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "deploy": "vercel"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
